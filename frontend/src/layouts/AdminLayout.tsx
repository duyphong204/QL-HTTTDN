import PortalShell from "@/layouts/PortalShell";

/** Layout cổng Quản trị — dùng trực tiếp nếu sau này tách route tree riêng cho ADMIN. */
const AdminLayout = () => <PortalShell portal="admin" />;

export default AdminLayout;
